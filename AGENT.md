# Agent instructions

The canonical repository instructions are in [`AGENTS.md`](AGENTS.md). Read and
follow that file before changing `bootwitness` code, persistence semantics,
tests, or Debian packaging.

This compatibility file exists because some integrations look for `AGENT.md`
while Codex and other repository-aware agents conventionally discover
`AGENTS.md`.
